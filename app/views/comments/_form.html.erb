<%= form_for([@listing, @listing.comments.build]) do |f| %>
<% if @listing.errors.any? %>
<div id="error_explanation">
  <h2>
    <%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>
  <ul>
    <% listing.errors.full_messages.each do |message| %>
    <li>
      <%= message %>
    </li>
    <% end %>
  </ul>
</div>
<% end %>
<p>
  <%= f.text_area :body, class: 'form-control', placeholder: 'Type your question here' %>
</p>
<br>
<p>
  <%= f.submit 'Submit Question', class: 'btn btn-success' %>
</p>
<% end %>