<div class="mt-3">
  <%= form_for :reply, url: listing_comment_reply_index_path({ listing_id: listing_id, comment_id: comment_id }) do |f| %>
    <p>
      <%= f.text_area :reply_body, class: 'form-control', placeholder: 'Type your reply here' %>
    </p>
    <p>
      <%= f.submit 'Reply', class: 'btn btn-warning' %>
    </p>
  <% end %>
</div>